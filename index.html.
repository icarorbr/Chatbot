<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IPTV Premium - Chatbot de Atendimento</title>
    <style>
        /* Estilo básico para a página */
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            color: #fff;
            text-align: center;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        h1 {
            margin-bottom: 20px;
        }

        .content {
            max-width: 800px;
            padding: 20px;
            margin-bottom: 30px;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
        }

        .chatbot {
            width: 100%;
            max-width: 400px;
            background-color: #fff;
            color: #333;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.4);
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        .chat-header {
            background-color: #2a5298;
            color: #fff;
            padding: 15px;
            text-align: left;
            font-weight: bold;
            border-bottom: 2px solid #1e3c72;
        }

        .chat-window {
            flex: 1;
            padding: 15px;
            height: 300px;
            overflow-y: auto;
            text-align: left;
            background-color: #f4f4f4;
        }

        .chat-message {
            margin: 5px 0;
            padding: 10px;
            border-radius: 10px;
            max-width: 80%;
            word-wrap: break-word;
        }

        .bot-message {
            background-color: #d1e7dd;
            align-self: flex-start;
        }

        .user-message {
            background-color: #cfe2ff;
            align-self: flex-end;
            text-align: right;
        }

        .chat-input {
            display: flex;
            border-top: 1px solid #ccc;
        }

        .chat-input input {
            flex: 1;
            padding: 10px;
            border: none;
            border-radius: 0;
            font-size: 16px;
            outline: none;
        }

        .chat-input button {
            background-color: #1e3c72;
            color: #fff;
            border: none;
            padding: 10px 15px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .chat-input button:hover {
            background-color: #16315e;
        }

        .cta-button {
            margin-top: 20px;
            padding: 15px 25px;
            background-color: #28a745;
            color: #fff;
            text-decoration: none;
            border-radius: 10px;
            font-size: 18px;
            transition: background-color 0.3s;
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.4);
        }

        .cta-button:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>

    <!-- Seção de Apresentação -->
    <div class="content">
        <h1>📺 IPTV Premium - Assista Tudo Sem Travamentos!</h1>
        <p>⚡ Filmes, séries, esportes e muito mais em alta qualidade.  
        🎯 Teste grátis disponível!  
        📡 Compatível com Smart TVs, TV Box, celulares e computadores. </p>
    </div>

    <!-- Chatbot -->
    <div class="chatbot">
        <div class="chat-header">🤖 Atendimento IPTV</div>
        <div class="chat-window" id="chatWindow">
            <div class="chat-message bot-message">Olá! 👋 Bem-vindo à IPTV Premium! Como posso te ajudar hoje?</div>
        </div>
        <div class="chat-input">
            <input type="text" id="userInput" placeholder="Digite sua mensagem...">
            <button onclick="sendMessage()">Enviar</button>
        </div>
    </div>

    <!-- Botão para ação direta -->
    <a class="cta-button" href="https://api.whatsapp.com/send?phone=5527999846791" target="_blank">
        📞 Fale Conosco pelo WhatsApp!
    </a>

    <!-- Script do Chatbot -->
    <script>
        const chatWindow = document.getElementById('chatWindow');
        const userInput = document.getElementById('userInput');

        // Função para enviar mensagens
        function sendMessage() {
            const message = userInput.value.trim();
            if (message === "") return;

            // Exibir mensagem do usuário
            appendMessage(message, "user-message");

            // Responder com base no conteúdo
            const response = getResponse(message);
            setTimeout(() => appendMessage(response, "bot-message"), 1000);

            // Limpar input
            userInput.value = "";
        }

        // Função para adicionar mensagem ao chat
        function appendMessage(text, className) {
            const messageDiv = document.createElement('div');
            messageDiv.className = `chat-message ${className}`;
            messageDiv.textContent = text;
            chatWindow.appendChild(messageDiv);
            chatWindow.scrollTop = chatWindow.scrollHeight;
        }

        // Função para analisar a mensagem e responder
        function getResponse(userMsg) {
            const msg = userMsg.toLowerCase();

            if (msg.includes("oi") || msg.includes("olá") || msg.includes("bom dia") || msg.includes("boa tarde") || msg.includes("boa noite")) {
                return "Olá! 😊 Tudo bem? Como posso te ajudar com o IPTV hoje?";
            }

            if (msg.includes("preço") || msg.includes("valor") || msg.includes("custa")) {
                return "Temos planos a partir de R$ 30,00/mês com acesso ilimitado a mais de 10.000 canais, além de conteúdos sob demanda. Quer conhecer mais detalhes?";
            }

            if (msg.includes("teste") || msg.includes("grátis") || msg.includes("degustação")) {
                return "Claro! Oferecemos um teste grátis por 24 horas para você conhecer o serviço. Gostaria de solicitar agora?";
            }

            if (msg.includes("planos") || msg.includes("assinatura")) {
                return "Oferecemos planos mensais, trimestrais e anuais. Qual seria o melhor para você?";
            }

            if (msg.includes("suporte") || msg.includes("problema") || msg.includes("erro")) {
                return "Nosso suporte técnico está disponível 24 horas! Clique no botão abaixo para falar direto pelo WhatsApp.";
            }

            if (msg.includes("conteúdo") || msg.includes("canais") || msg.includes("filmes") || msg.includes("séries")) {
                return "Temos mais de 10.000 canais, incluindo esportes, filmes, séries e conteúdo adulto (opcional). Quer uma lista completa?";
            }

            if (msg.includes("obrigado") || msg.includes("valeu") || msg.includes("agradeço")) {
                return "De nada! 😊 Qualquer dúvida, é só chamar!";
            }

            return "Desculpe, não entendi sua pergunta. 😕 Pode reformular ou, se preferir, clique no botão abaixo para falar direto pelo WhatsApp.";
        }

        // Enviar mensagem ao pressionar Enter
        userInput.addEventListener("keypress", function (e) {
            if (e.key === "Enter") {
                sendMessage();
            }
        });
    </script>

</body>
</html>